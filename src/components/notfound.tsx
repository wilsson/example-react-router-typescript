import * as React from 'react';

export const NotFound = (props) => {
    console.log('NotFound>', props.match)
    return(
        <div id='NotFound'>
            Page NotFound s
        </div>
    )
};